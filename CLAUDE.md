# Claudecode- プロジェクト

## 作業ルール

- 作業ブランチ: `claude/add-desktop-save-location-IuItE`
- 作業が完了したら必ずコミット・プッシュする
- ファイル操作はこのリポジトリ配下のみ行う

## プロジェクト構成

- `index.html` — 散らかったデスクトップのシミュレーション（右クリック保存ダイアログ付き）
- `営業リスト_名刺一覧.xlsx` — 名刺20枚から生成した営業リスト

## 重要な注意事項

- 大きな変更をする前に必ずプランモードで計画を提示すること
- 外部ファイルやシステム全体には触れない（このフォルダ内のみ）
- コンテキストが長くなったら `/compact` で圧縮する

## よく使うコマンド

```bash
git add <file> && git commit -m "..." && git push
```
